'use strict';

angular.module('booksterApp')
    .factory('Copy', function ($resource, DateUtils) {
        return $resource('api/copys/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
